// This file is intentionally simplified.
// User creation is now handled by the /api/auth/register endpoint.
// This route file is kept for any future user-specific routes.
const express = require('express');
const router = express.Router();

module.exports = router;
